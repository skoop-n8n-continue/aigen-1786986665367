const fs = require('fs');
let html = fs.readFileSync('index.html', 'utf8');

// The best way to format these is to use explicit HTML structures for multi-price.
// I will rewrite the menu items in index.html to match the layout.

